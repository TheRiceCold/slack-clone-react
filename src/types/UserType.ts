type UserType = {
  id: Number,
  provider: string,
  uid: string,
  allow_password_change: string,
  name: null,
  nickname: null,
  image: null,
  email: string,
  created_at: string,
  updated_at: string,
};

export default UserType;
